# Current Feature

## Status

<!-- Not Started | In Progress | Complete -->

## Goals

<!-- Bullet points of what success looks like -->

## Notes

<!-- Additional context, constraints, or details from spec -->

## History

<!-- Keep this updated. Earliest to latest -->

- 2026-08-19: Initial Next.js + Tailwind CSS v4 project setup (`aaf1da7`), pushed to `origin/master`.
- 2026-08-20: Added dashboard UI screenshots as a design reference and linked them from the UI/UX section of `project-overview.md` (`9701c69`).
- 2026-08-20: Added `src/lib/mock-data.ts` — single source of truth for dashboard data until the database is in place: current user, seven system item types, six collections, twenty items. Relationships held on the item via `typeId` / `collectionIds`, no helper functions (`f53ab9f`). Build passed; both commits pushed to `origin/master`.
- 2026-08-20: Added the three dashboard UI phase specs under `context/features/` (`7b00d74`).
- 2026-08-20: Completed Dashboard UI Phase 1 (@context/features/dashboard-phase-1-spec.md) (`ab3738a`). Initialized ShadCN with the Nova preset (`radix-nova`, Lucide, Geist, neutral base — the CLI now uses named presets instead of `--base-color`) and added the `button` and `input` components. Added the `/dashboard` route with a shell layout: placeholder sidebar, display-only top bar (search + New Item), and a scrollable main area. Dark mode is a hardcoded `dark` class plus `color-scheme: dark`, not `next-themes`. Geist wired to `--font-sans` / `--font-mono`, which the preset expects but does not define. No `tailwind.config.ts` — v4 CSS config left intact. Deferred as out of spec: the "New Collection" button, the sidebar-collapse toggle, and redirecting `/` to `/dashboard`. Build and lint passed; verified in the browser.
- 2026-08-20: Completed Dashboard UI Phase 2 (@context/features/dashboard-phase-2-spec.md) (`2edd188`). Replaced the placeholder sidebar with ShadCN's `sidebar` (`collapsible="icon"`), which also pulled in `avatar`, `sheet`, `tooltip`, `separator` and `skeleton`. `Sidebar.tsx` stays a server component: brand header, a Types group (all seven types, lucide icon in the type's own color, item-count badge, linking to `/items/[label]`), then Favorites and Recent Collections groups linking to `/collections/[id]` — none of those routes exist yet. `SidebarUser.tsx` is the footer (initials fallback since `currentUser.image` is null, display-only settings gear). `src/lib/icons.ts` maps the mock data's `icon` string to its lucide component. `SidebarProvider` / `SidebarInset` in the dashboard layout, wrapped in `TooltipProvider` for the collapsed-mode tooltips; `SidebarTrigger` added to the top bar. Below 768px the sidebar becomes a Sheet drawer automatically; state persists in a cookie and ⌘B/Ctrl+B toggles. Three decisions to revisit: type icon colors use an inline `style` (a data-driven hex — the no-inline-styles rule would otherwise force a hardcoded hex→class table that stops tracking the source of truth); Recent excludes favorites so a collection never appears twice; and ShadCN's generated `use-mobile` hook was rewritten onto `useSyncExternalStore` because it set state inside an effect and failed `react-hooks/set-state-in-effect`. Deferred as out of spec: the "New Collection" button and the collapsible Types/Collections section chevrons in the screenshot. Build and lint passed; verified in the browser.
- 2026-08-20: Completed Dashboard UI Phase 3 (@context/features/dashboard-phase-3-spec.md) (`671ed3c`). The dashboard main area, all server components, each section querying `mock-data` itself the way `Sidebar.tsx` does. `StatsCards.tsx` (items, collections, favorite items, favorite collections), `RecentCollections.tsx` + `CollectionCard.tsx` (six most recently updated, card links to `/collections/[id]`, left accent = dominant item type, footer row of the type icons the collection contains), `PinnedItems.tsx` and `RecentItems.tsx` sharing `ItemRow.tsx` (type tile, title with pin/star, description, tag badges, date). `src/lib/dashboard.ts` holds the shared derivations: type lookup, items in a collection, a collection's distinct types ordered by frequency, a `byNewest` comparator, and a UTC-pinned `Jan 15` formatter (UTC so server and client render the same string). Added ShadCN `card` and `badge`. Three decisions to revisit: item rows are not clickable — items open in the quick-access drawer, which is a later feature, and `/items/[id]` would collide with Phase 2's `/items/[label]` type routes; type colors again use inline `style` for the same data-driven-hex reason as Phase 2; and Recent Collections here includes favorites (the sidebar excludes them to avoid double-listing) since the spec asks for recent collections and the screenshot shows all six. "View all" points at `/collections`, which does not exist yet. Note the dark `--card` token sits lighter than the background, so cards read more raised than the near-black cards in the screenshot. Build and lint passed; dashboard verified server-rendering over the dev server.

- 2026-08-21: Completed Prisma + Neon PostgreSQL Setup (@context/features/database-spec.md) (`eec9de3`). Prisma 7.9.1 schema (`prisma/schema.prisma`) covering `User`, `Account`, `Session`, `VerificationToken`, `ItemType`, `Item`, `Collection`, `ItemCollection`, `Tag`, `ItemTag` — mirrors the `project-overview.md` draft plus `VerificationToken` (required by NextAuth, not in the draft) and `@@index` on every foreign key. Uses Neon's recommended `@prisma/adapter-neon` (WebSocket driver): `src/lib/prisma.ts` is a dev-mode singleton on the pooled `DATABASE_URL`, while `prisma.config.ts` points CLI/migration commands at a direct `DATABASE_URL_UNPOOLED` (schema's `datasource` has no `url`). First migration (`prisma/migrations/20260821152535_init`) applied and confirmed in sync via `prisma migrate status`. Added `scripts/test-db.ts` (`npm run db:test`) as a connection smoke test. Infra-only — `src/lib/mock-data.ts` still powers the dashboard; wiring components to the database is a separate future feature. Two things worth knowing: Prisma 7 forced `"type": "module"` into `package.json` and dropped its bundled Rust query engine (client output now generated to gitignored `src/generated/prisma` via a `postinstall: prisma generate` script); and `DATABASE_URL_UNPOOLED` was derived from the given pooled connection string by dropping `-pooler` from the hostname (Neon's standard convention), confirmed correct when the migration ran successfully. Build and lint passed.

- 2026-08-21: Completed Seed Data (@context/features/seed-spec.md) (`98b8f92`). Added `prisma/seed.ts`, reusing the `src/lib/prisma.ts` singleton, to populate a demo user (`demo@devstash.io`, `bcryptjs`-hashed password, `isPro: false`), all seven system `ItemType`s, and five collections (React Patterns, AI Workflows, DevOps, Terminal Commands, Design Resources) with 18 items total, matching the spec's counts and content types exactly — verified against the dev database. Registered the seed command in `prisma.config.ts`'s `migrations.seed` (Prisma 7 moved this out of `package.json`'s `prisma.seed` field) and added `npm run db:seed`. Two things worth knowing: `ItemType`'s `@@unique([userId, name])` has a nullable `userId`, so system types are looked up with `findFirst({ where: { userId: null, name } })` + `create` rather than `upsert`, since Prisma's generated compound-unique input doesn't accept `null` there; and the seed is idempotent for the user and system types but not for collections/items — re-running it duplicates collections, worth revisiting if it needs to run repeatedly (e.g. after `prisma migrate reset`). Build and lint passed.

- 2026-08-21: Completed Dashboard Collections (@context/features/dashboard-collections-spec.md) (`9993a79`). Added `src/lib/db/collections.ts` with `getRecentCollections` and `getCollectionStats`, scoped to the seeded demo user since auth isn't wired up yet. `RecentCollections.tsx` is now an async server component querying Prisma directly instead of `mock-data`; `CollectionCard.tsx` consumes the new `CollectionSummary` shape (dominant-type border accent, per-type icon row, real item counts). `StatsCards.tsx`'s Collections/Favorite Collections stats now come from the database — Items/Favorite Items stay on mock data since items aren't migrated yet. `ItemTypeIcon`/`ItemTypeTile` were decoupled from `mock-data`'s `ItemType` to a minimal `IconableType` so they work with both data sources. Verified against the seeded dev database via the server-rendered HTML (no headless-browser tool was available in this environment): all five collections rendered with correct item counts, descriptions, and dominant-type colors/icons. Build and lint passed.

- 2026-08-21: Completed Dashboard Items (@context/features/dashboard-items-spec.md) (`0c9a461`). Added `src/lib/db/items.ts` with `getPinnedItems`, `getRecentItems` and `getItemStats`, scoped to the seeded demo user, mirroring `collections.ts`'s pattern. `PinnedItems.tsx`/`RecentItems.tsx` are now async server components querying Prisma directly, with sorting/limiting pushed into the query instead of client-side `Array.sort`/`slice`; `ItemRow.tsx` consumes the new `ItemSummary` shape (type-derived icon/border, real tags, real dates) instead of resolving `typeId` against mock data. `StatsCards.tsx`'s Items/Favorite Items stats now come from the database too, so all four stat cards are DB-backed. `formatItemDate` was changed to take a `Date` instead of an ISO string since Prisma returns `Date` objects. One thing worth knowing: `getItemType`/`itemsInCollection`/`collectionTypes`/`byNewest` in `src/lib/dashboard.ts` are now unused but left in place, since `Sidebar.tsx` still depends on other `mock-data` exports and hasn't been migrated yet. Verified against the seeded dev database via the server-rendered HTML: 18 items / 5 collections stat counts, real item titles/dates/tags, correct per-type border colors, and the Pinned section correctly absent since no seeded items are pinned. Build and lint passed.

- 2026-08-21: Completed Stats & Sidebar (@context/features/stats-sidebar-spec.md) (`c207230`). Added `getItemTypesWithCounts` to `src/lib/db/items.ts`, returning the seven system item types with real per-type item counts for the demo user, ordered by a fixed `SYSTEM_TYPE_ORDER` list since `ItemType` has no ordering column and `cuid()` ids aren't reliably time-sortable. Extracted `toCollectionSummary` out of `getRecentCollections` in `src/lib/db/collections.ts` and reused it in two new functions, `getFavoriteCollections` and `getRecentNonFavoriteCollections`. `Sidebar.tsx` is now an async server component querying all three instead of reading `mock-data`; its Recent Collections rows show a colored circle (dominant item type's color) instead of an item-count badge, while Favorites rows keep the star badge unchanged. Added a "View all collections" link under the collections list, pointing at `/collections`. `SidebarUser.tsx` still reads `currentUser` from `mock-data` for the profile name/email/avatar — out of scope for this spec, so `mock-data.ts` stays in place. Verified against the seeded dev database via the server-rendered HTML: type counts (4/3/5/0/0/0/6 for snippet/prompt/command/note/file/image/link) matched the seed data, all five Recent Collections rows rendered distinct dominant-type colors, the new link was present, and the Favorites group correctly stayed hidden since no seeded collection is marked favorite. Build and lint passed.

- 2026-08-22: Completed Add Pro Badge to Sidebar (@context/features/add-pro-badge-sidebar.md) (`65222bb`). Added a subtle "PRO" badge (ShadCN `Badge`, `outline` variant) next to the File and Image rows in the sidebar's Types group, in `src/components/dashboard/Sidebar.tsx`. A `PRO_TYPE_NAMES` set (`"file"`, `"image"`) is checked against each type's lowercase `name` (not the pluralized `label` used for display/routing), and the badge renders inline next to the label inside the type's `Link` rather than in the `SidebarMenuBadge` slot — that slot already holds the per-type item count and is absolutely positioned to the row's right edge, so a second badge there would have overlapped it. Sized down (`h-4`/`text-[10px]`/`text-muted-foreground`) to stay visually subtle and distinct from the primary-colored count badge. Verified against the already-running dev server's rendered HTML: the badge appears only on Files/Images, not the other five types. Build and lint passed.

- 2026-08-22: Completed Codebase Cleanup Quick Wins (`ff56e5b`). Three low-risk fixes picked from that day's codebase-scanner audit, excluding auth (not implemented yet) and anything touching the Prisma schema/migrations. Removed four unused exports (`getItemType`, `itemsInCollection`, `collectionTypes`, `byNewest`) from `src/lib/dashboard.ts`, left over from the pre-Prisma `mock-data.ts` era — only `formatItemDate` remains. Extracted the duplicated `getDemoUserId()` (previously defined separately in both `src/lib/db/collections.ts` and `src/lib/db/items.ts`) into a single `src/lib/db/user.ts`, wrapped in React's `cache()` so a dashboard load no longer re-queries the same demo user row from every section. Split `Sidebar.tsx` (~115 lines) into `SidebarTypesNav.tsx` and `SidebarCollectionsNav.tsx` — a pure extraction with no behavior change; `Sidebar.tsx` now just fetches data and composes the shell plus these two. `toLabel`'s duplication between `collections.ts` and `items.ts` was left as-is since it wasn't in scope. Verified against the seeded dev database via the server-rendered dashboard HTML (200 response, sidebar sections all present). Build and lint passed.

- 2026-08-23: Completed Auth Setup - NextAuth + GitHub Provider, Phase 1 (@context/features/auth-phase-1-spec.md) (`83c1bf0`). Installed `next-auth@beta` (5.0.0-beta.32) and `@auth/prisma-adapter`. Split config pattern per NextAuth v5's edge-compatibility guidance (verified against current docs via Context7): `src/auth.config.ts` holds only the GitHub provider (edge-safe, no adapter), `src/auth.ts` adds `PrismaAdapter` (reusing the existing `src/lib/prisma.ts` singleton) and forces `session: { strategy: 'jwt' }`, with `jwt`/`session` callbacks threading `user.id` onto the session — typed via a `Session` module augmentation in `src/types/next-auth.d.ts`. `src/proxy.ts` matches only `/dashboard/:path*` and redirects unauthenticated requests to `/api/auth/signin` with the original URL as `callbackUrl`; NextAuth's default sign-in page is used (no custom `pages.signIn`). `src/app/api/auth/[...nextauth]/route.ts` exports the handlers. Documented `AUTH_SECRET`/`AUTH_GITHUB_ID`/`AUTH_GITHUB_SECRET` in `.env.example` (all three were already set in `.env`). Verified via Playwright against the dev server: `/dashboard` redirects to sign-in with the correct `callbackUrl`, and clicking "Sign in with GitHub" reaches GitHub's OAuth authorize screen with the correct `client_id`/`redirect_uri`. One thing worth knowing: the full round-trip (completing a real GitHub login and landing back on `/dashboard`) was not verified end-to-end since no test GitHub credentials were available in this environment — worth a manual check. Build and lint passed.

- 2026-08-23: Completed Auth Credentials - Email/Password Provider, Phase 2 (@context/features/auth-phase-2-spec.md) (`9d649e3`). Added a bcrypt-backed Credentials provider alongside Phase 1's GitHub OAuth: `src/auth.config.ts` gets a Credentials placeholder (`authorize: () => null`, edge-safe), `src/auth.ts` overrides it with real validation (`prisma.user.findUnique` by email, `bcrypt.compare` against the stored hash, cost-12 hash to match `seed.ts`), returning a sanitized user object so the password hash never reaches the session. The `User.password` field already existed in the schema from the original draft, so no migration was needed. Added `POST /api/auth/register` (`src/app/api/auth/register/route.ts`): Zod-validated (name/email/password min 8/confirmPassword via `.refine`), 409 on duplicate email, bcrypt hash, `{ success, data|error }` response shape. `zod` was added as a new dependency — not previously used in the codebase, but required by `coding-standards.md`'s "validate all inputs with Zod" rule. One thing worth knowing: `auth.ts` merges providers by filtering `authConfig.providers` for `typeof provider === "function" || provider.id !== "credentials"` before appending the real Credentials config — needed because `GitHub` is passed uninstantiated (a function) while `Credentials(...)` is a config object, so a plain `.id` check doesn't type-check against the union. Verified via curl (success, duplicate email, mismatched/short password, invalid email all return correct status/error) and Playwright (credentials sign-in reaches an authenticated `/dashboard`; GitHub OAuth still redirects to its authorize screen correctly). Build and lint passed.

- 2026-08-23: Completed Auth UI - Sign In, Register & Sign Out, Phase 3 (@context/features/auth-phase-3-spec.md) (`a4b5eec`). Replaced NextAuth's default pages with custom ones: `/sign-in` (email/password + GitHub via server actions in `src/actions/auth.ts`, `signInWithCredentials` using `useActionState`, error surfaced as a toast) and `/register` (name/email/password/confirm, Zod-validated client-side via a schema now shared with the API route through `src/lib/validations/auth.ts`, posts to `/api/auth/register`, redirects to `/sign-in?registered=1`). `src/proxy.ts` now redirects unauthenticated requests to `/sign-in` instead of `/api/auth/signin`, and its matcher also protects the new `/profile/:path*`. `SidebarUser.tsx` is now an async server component reading the real session via `auth()` instead of `mock-data`'s `currentUser`: avatar/name/email sit inside a `DropdownMenu` with a "Sign out" item (a `<form action={signOutAction}>`), and the settings-gear `SidebarMenuAction` now links to a new minimal `/profile` page instead of being inert. `UserAvatar.tsx` (`src/components/auth/`) is the reusable image-or-initials avatar, used by both the sidebar and `/profile`. Added shadcn's `dropdown-menu`, `label`, and `sonner` components; `sonner`'s generated wrapper pulls in `next-themes` by default, which this project doesn't use (dark mode is a hardcoded class, per Phase 2 of the dashboard UI) — hardcoded it to `theme="dark"` and removed the `next-themes` dependency instead. `lucide-react`'s installed version (1.33) dropped brand/logo icons, so the GitHub mark on the "Sign in with GitHub" button is an inline SVG rather than a lucide import. Verified via Playwright against the dev server: `/sign-in` renders and enforces the `callbackUrl` redirect chain from `/dashboard`, invalid credentials show an error toast, valid credentials and a freshly-registered account both reach `/dashboard`, the sidebar dropdown's "Sign out" returns to `/sign-in`, `/profile` renders the session user, and the GitHub button reaches GitHub's real OAuth authorize screen. One thing worth knowing: Playwright's role-based `.click()` intermittently missed the credentials/register forms right after navigation (a hydration-timing race in the test tool, not the app — confirmed by dispatching the same click via raw DOM `.click()`, which worked every time); real user interaction isn't affected. Build and lint passed.

- 2026-08-23: Completed Email Verification on Register (`66e4cdc`). Credentials-provider sign-ups now must click an emailed link before they can sign in; GitHub OAuth is untouched since the check lives only in the Credentials `authorize` path. `src/lib/tokens.ts`'s `createVerificationToken` reuses NextAuth's existing `VerificationToken` model (no migration) for a 24h single-use token, clearing any stale token for that email first. `src/lib/resend.ts` / `src/lib/email/send-verification-email.ts` send the link via Resend from `onboarding@resend.dev` — confirmed against current Resend docs that this sandbox sender needs no domain verification but can only deliver to the Resend account's own email, so arbitrary registrations locally won't actually receive mail (registration still succeeds regardless; the send failure is caught and logged, not fatal). `POST /api/auth/register` now mints the token and sends the email after creating the user, building the link from the request's own origin (no new base-URL env var). New `GET /api/auth/verify-email` validates the token, sets `User.emailVerified` and deletes the token in one transaction, then redirects to `/sign-in?verified=1` (or `?verifyError=1` if missing/expired). `src/auth.ts` throws a new `EmailNotVerifiedError` (a `CredentialsSignin` subclass) when `emailVerified` is null — checked only after the password compare succeeds, so it can't be used to probe account existence — and `src/actions/auth.ts` catches it to show "Please verify your email before signing in..." instead of the generic invalid-credentials toast. `/sign-in` gained `verified`/`verifyError` banners and its `registered=1` message now mentions checking email. One thing worth knowing: there's no resend-verification-email path, so an expired/lost link leaves a user stuck (can't re-register on a taken email) — deferred as a known gap rather than a required goal. Verified end-to-end against the dev database via Playwright + Neon MCP: register → token row created with correct 24h expiry → sign-in blocked with the verify toast → hitting the verify link sets `emailVerified` and consumes the token → sign-in then succeeds and reaches `/dashboard`; a bogus token correctly redirects to `verifyError=1`. Build and lint passed. Also added (separately, `fbffb45`) `scripts/delete-non-demo-users.ts` (`npm run db:clean-users`, dry-run by default, `--yes` to execute) which deletes every `User` except `demo@devstash.io` and their content via cascading deletes — used once this session to clear 5 leftover test accounts from the dev database.

- 2026-09-06: Completed Toggle Email Verification Requirement (`5be9045`). Added `EMAIL_VERIFICATION_ENABLED` (default enabled) so the emailed-link requirement from the Email Verification feature (`66e4cdc`) can be disabled locally, since Resend has no domain linked yet and its sandbox sender can only deliver to the Resend account's own email. `src/lib/email-verification.ts`'s `isEmailVerificationEnabled()` is the single read of the env var, used in both `POST /api/auth/register` (skips minting the `VerificationToken` and calling `sendVerificationEmail`, stamps `emailVerified: new Date()` at creation instead) and `src/auth.ts`'s credentials `authorize` (skips throwing `EmailNotVerifiedError`). GitHub OAuth untouched either way. Verified against the seeded dev database via Neon MCP + curl against NextAuth's credentials callback for both states: disabled — `emailVerified` stamped, no token row, sign-in succeeded immediately; enabled — `emailVerified` null, token row created, sign-in blocked with `code=email_not_verified` (unchanged from before). Build and lint passed.

- 2026-09-06: Completed Forgot Password (`771a009`). Added a "Forgot password?" link on `/sign-in`, a new `/forgot-password` page, and a new `/reset-password?token=...` page, reusing the existing `VerificationToken` model with no migration. `src/lib/tokens.ts`'s new `createPasswordResetToken` scopes reset tokens under a `reset:${email}` identifier (a new `PASSWORD_RESET_TOKEN_PREFIX` export) rather than the plain-email identifier `createVerificationToken` uses — this was the key design decision, since the table's `token` column is globally unique across both token kinds and reusing the plain-email scheme would let a password reset silently invalidate a pending email-verification token for the same address (or vice versa); the prefix also lets `POST /api/auth/reset-password` reject a stolen/misrouted email-verification token outright by checking `identifier.startsWith(...)` before trusting it. Reset tokens are shorter-lived than verification tokens (1h vs 24h). `POST /api/auth/forgot-password` always responds `{ success: true }` regardless of whether the email exists, matching `send-verification-email.ts`'s Resend pattern via a new `sendPasswordResetEmail`; GitHub-only accounts (`password: null`) are silently skipped — no email sent, same generic response — so the endpoint never leaks account existence or auth method. `POST /api/auth/reset-password` validates the token (prefix + expiry), bcrypt-hashes the new password (cost 12, matching `seed.ts`/`auth.ts`), and updates `User.password` + deletes the token in one transaction. `ForgotPasswordForm`/`ResetPasswordForm` follow `RegisterForm`'s client-side fetch pattern (not the server-action pattern `SignInForm` uses); `/sign-in` gained a `reset=1` confirmation banner alongside the existing `verified`/`verifyError` ones. Works independently of `EMAIL_VERIFICATION_ENABLED`, per the goal. Two known gaps, deliberately left out of scope: sessions are JWT-based with no server-side store, so an existing signed-in session elsewhere isn't revoked when a password is reset (would need a `passwordChangedAt`-style check in the `jwt` callback); and `/api/auth/forgot-password` has a minor timing side-channel — the branch for an account with a password does a token write + awaited email send before responding, while the "no such account"/"GitHub-only" branches return immediately, so response time alone could hint at account existence despite the identical body. Verified end-to-end against the dev database via Neon MCP + curl: token minted with the correct `reset:` prefix and ~1h expiry, consumed exactly once (reuse and a bogus token both correctly rejected), the new password authenticated via NextAuth's credentials callback, and the demo user's password was restored to its seeded value afterward with no lasting side effects. Build and lint passed.

- 2026-09-06: Completed Profile Page (@context/features/profile-spec.md) (`32ec1ec`). Expanded `/profile` (already protected by `src/proxy.ts`'s `/profile/:path*` matcher) from a minimal name/email/avatar card into the full account page: `src/lib/db/profile.ts`'s `getProfileAccount(userId)`/`getProfileStats(userId)` are the first DB query functions in the codebase parameterized by an explicit `userId` rather than the dashboard's hardcoded demo-user pattern, since `/profile` already has a real session via `auth()`. `ProfileStats.tsx` shows item/collection totals plus a per-type breakdown (reusing `ItemTypeIcon`); `ChangePasswordForm.tsx` (client, `useActionState`, mirrors `SignInForm`'s pattern) and `DeleteAccountDialog.tsx` (shadcn `AlertDialog`) drive two new server actions in `src/actions/profile.ts` — `changePasswordAction` (bcrypt-verifies the current password, cost-12 hashes the new one) and `deleteAccountAction` (cascading `prisma.user.delete` + `signOut`) — chosen over API routes since, unlike register/reset-password, these mutate an already-authenticated session and coding-standards.md reserves Server Actions for that case. The Change Password card only renders when `getProfileAccount` reports `hasPassword: true`, so GitHub-only accounts don't see it. Added shadcn's `alert-dialog` component; its CLI-fetched template had a broken `import { cn } from "cn"` (should be `@/lib/utils`), which also silently added a stray `cn` npm package — fixed the import and `npm uninstall cn`'d the dependency. One thing worth knowing: the actual change-password and delete-account flows were not exercised end-to-end since both mutate/destroy the seeded demo user, unlike the reset-password feature which could restore state afterward — verified instead via build/lint and by reading the server-rendered HTML (correct name, email, "Member since" date, and 18/5 item/collection counts with a 4/3/5/0/0/0/6 type breakdown matching the seed data) confirmed via curl against a real credentials-signed-in session. Build and lint passed.

- 2026-09-06: Completed Rate Limiting for Auth (@context/features/rate-limiting-spec.md) (`c8dd80e`). Added `src/lib/rate-limit.ts`: `checkRateLimit(prefix, identifier, limit, window)` lazily builds and caches one Upstash `Ratelimit` (sliding window) per prefix, failing open (`success: true`) both when `UPSTASH_REDIS_REST_URL`/`UPSTASH_REDIS_REST_TOKEN` are unset and when the Upstash call throws; `getClientIp` reads `x-forwarded-for`; `rateLimitResponse`/`rateLimitExceededMessage` share the "Too many attempts, try again in X minutes" text between HTTP responses and non-HTTP callers. `register`, `forgot-password`, and `reset-password` each gained an identical IP-keyed guard at the top of `POST`, before body parsing. Built `POST /api/auth/resend-verification` from scratch (IP+email keyed) since the spec assumed it already existed — it didn't, a known gap left by the Email Verification feature (`66e4cdc`) — mirroring `forgot-password`'s always-`{ success: true }` pattern so it never leaks account existence or verification state; `SignInForm.tsx` now offers a "Resend email" action on the `email_not_verified` toast, wired to it. The key design decision was where login rate limiting lives: not at `/api/auth/callback/credentials` (NextAuth's internal route), since the app's own sign-in never calls that route over HTTP — `SignInForm` submits to the `signInWithCredentials` Server Action, which calls `signIn()` in-process — so the IP+email check (5/15min) lives in that action instead, using `headers()` from `next/headers`, and surfaces failures through the same `{ success, error }` toast pattern as its other errors rather than a literal 429/`Retry-After` (that HTTP semantic doesn't apply to a Server Action). One pre-existing bug surfaced and fixed during review: `ForgotPasswordForm.tsx` was discarding the response body on any non-2xx status and showing a generic "Something went wrong" instead of the real message — harmless before this feature since `forgot-password` never returned a specific error, but it would have swallowed the new rate-limit message; changed to parse `body.error` like `RegisterForm`/`ResetPasswordForm` already do. Verified via curl against the dev server with no Upstash credentials configured (confirms fail-open): all four routes validate and respond correctly, invalid-input 400s and bogus-token 400s still behave as before. No Playwright tool was available this session, so the full browser sign-in flow (including the resend-email toast action) wasn't re-verified end-to-end — worth a manual check before relying on it in production. Build and lint passed.

- 2026-09-07: Completed Items List View (@context/features/item-list-view-spec.md) (`dcca192`). Added the dynamic route `/items/[type]` (`src/app/items/[type]/page.tsx`), the first dynamic-segment page in the app — an async server component using Next 16's generated `PageProps<"/items/[type]">` for the awaited `params`. `src/lib/db/items.ts` gained `getItemsByTypeSlug(slug)`, mirroring `getRecentItems`: it fetches the seven system `ItemType`s and matches the incoming slug against `toLabel(name).toLowerCase()` (so `"snippets"` → the `snippet` type) rather than doing string surgery on the slug, then returns `{ type, items }` (demo-user scoped, `updatedAt` desc, via the shared `toItemSummary`) or `null` for an unknown slug, which the page turns into `notFound()`. New `src/components/items/ItemCard.tsx` is the grid card the spec asked for instead of reusing `ItemRow` — type tile, title with pin/star, date, `line-clamp-2` description, tag badges, and a left border in the type's color via inline `style` (the same data-driven-hex reason `ItemRow`/`ItemTypeIcon` already use inline styles). Grid is `grid-cols-1 md:grid-cols-2` per the spec's "two columns on medium and up". The page follows the standalone `/profile` pattern — no dashboard sidebar shell, just a type-icon header with item count, the grid (or a "No <type> yet." empty state), and a "Back to dashboard" link. One thing worth knowing: `src/proxy.ts`'s matcher was extended to `/items/:path*` so the new page is auth-gated like `/dashboard` and `/profile` — not in the spec's bullet list, but every sibling app page is gated and the spec says to follow existing patterns. Verified via Playwright against the dev server: `/items/snippets` (4 cards) and `/items/commands` (5 cards, left border `rgb(249,115,22)` = `#f97316`, grid `450px 450px` at 1040px width), `/items/notes` (empty state), `/items/bogus` (404). Build and lint passed.

- 2026-09-07: Completed Vitest Setup (`5320063`) and Items List View — Three-Column Grid (`ebd99b4`), merged together via `edba656`. **Vitest**: added `vitest.config.ts` (`node` environment, `vite-tsconfig-paths` for the `@/*` alias) with test discovery restricted to `src/actions/**/*.test.ts` and `src/lib/**/*.test.ts` so components and RSCs can't be unit-tested — the project's chosen scope is server actions and utilities only. Added `npm test` / `npm run test:watch`; new devDeps `vitest` + `vite-tsconfig-paths`. Pinned **Vitest 3** (3.2.7), not 5, because Vitest 5's peer requires `@types/node >=22` while the project pins `^20`; the Node runtime is actually 24, so bumping `@types/node` to `^24` (which also unlocks Vitest 5) is a deferred follow-up rather than folded into this change. 26 tests across 5 files: `formatItemDate`, the auth Zod schemas (`registerSchema` etc.), `rate-limit.ts` helpers (`getClientIp`, `rateLimitExceededMessage` pluralization, `checkRateLimit` fail-open with no Upstash env), `isEmailVerificationEnabled`, and `signInWithCredentials` — the last demonstrating the `vi.mock` module-boundary pattern (stub `@/auth`, `next/headers`, and the `next-auth` `AuthError`/`CredentialsSignin` classes). Test helpers are imported explicitly from `vitest` (no globals), so `tsconfig.json` was left untouched; `next build` type-checks the `.test.ts` files since they sit under `src/**`, and they're tree-shaken from the app bundle as nothing imports them. Documented the testing scope/workflow in `CLAUDE.md`, `context/coding-standards.md` (new Testing section), `context/ai-interaction.md` (new Testing section + workflow step 4 now runs `npm test`), and the feature skill's action table (new `test` action row, an edit the user made). `npm install` flagged 6 vulnerabilities in Vitest's transitive dev-only deps (esbuild/vite chain) — not acted on. **Three-Column Grid**: one-line change in `src/app/items/[type]/page.tsx` — the items grid goes `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3` (1 → 2 → 3), up from `grid-cols-1 md:grid-cols-2`; `ItemCard` untouched. Verified via Playwright: 3 columns at 1440px (`330px × 3`), 2 at 800px, 1 at 500px. Both changes were stacked on one branch (`feature/vitest-setup`) at the user's direction and committed separately. Build, lint, and all 26 tests passed.

- 2026-09-08: Completed Item Drawer (@context/features/item-drawer-spec.md) (`32891bc`, merged `92dfe67`). Right-side shadcn `Sheet` that is the item detail view — no separate item page. `src/lib/db/items.ts` gained `getItemDetail(id)` (+ `ItemDetail` type): the `ItemSummary` fields plus `contentType`, `content`, `url`, `language`, the three `file*` fields, `createdAt`, and `collections` (`{id,name}[]`); demo-user scoped via `getDemoUserId()` like every other query here, returning `null` when the id isn't the demo user's. New `GET /api/items/[id]` (`src/app/api/items/[id]/route.ts`) — first route under `/api` that isn't auth-related — does its own `auth()` check (401) since `proxy.ts`'s matcher only covers pages, then `getItemDetail` → 404 or `{ success, data }`. Client architecture: `ItemDrawerProvider` (`src/components/items/`, client) holds `open`/`detail`/`loading`/`error` state and exposes `useItemDrawer().openItem(id)`, which `fetch`es the API on click (a `requestRef` counter discards a slow earlier response; `createdAt`/`updatedAt` are revived from JSON strings back to `Date`), and renders `<ItemDrawer>`; the dashboard `page.tsx` wraps `PinnedItems`+`RecentItems` in it, `items/[type]/page.tsx` wraps the card grid. `ItemDrawerTrigger` (client) is a thin `<button>` wrapper so `ItemCard` and `ItemRow` stay server components — they were previously non-clickable placeholders, now each is wrapped in the trigger. `ItemDrawer` (client) renders: type tile + title, type-label + language badges, the action bar, then Description / Content (a mono `<pre>`, or a URL link for `link`-type items, or filename+size for files) / Tags / Collections / Details (Created + Updated via a new `formatLongDate` in `src/lib/dashboard.ts` — "January 15, 2024", UTC-pinned, with tests). Action bar per the screenshot — Favorite (amber `fill` when `detail.isFavorite`), Pin, Copy, then right-aligned Edit + Delete (red trash). Only **Copy** is wired (`navigator.clipboard` + sonner toast); Favorite/Pin/Edit/Delete render but their mutations are a later feature, per the spec's "wiring is not the focus" note. Loading shows `Skeleton`s in the header and body; a fetch failure shows an error state. Radix's dialog-needs-a-description warning is silenced with an `sr-only` `SheetDescription`. Verified via Playwright against the seeded dev database: snippet card → drawer with code content, `Snippets`+`typescript` badges, `React Patterns` collection, `August 21, 2026` dates; dashboard rows → command item (no language badge, `npm outdated` content) and link item (URL section instead of Content); Copy → toast; `GET /api/items/does-not-exist` → `404 {success:false,error:"Item not found"}`; no console errors/warnings. Build, lint, and all 28 tests passed.

- 2026-09-08: Completed Item Drawer — Edit Mode (@context/features/item-drawer-edit-spec.md) (`75d5055`, merged `085f87d`). The Edit button in the item drawer now swaps the view for an inline edit form in the same drawer; Save persists, Cancel discards. New `src/actions/items.ts` — the first item mutation server action — `updateItem(itemId, input)`: `auth()` gate (signed-in check only, not a real per-user scope since the dashboard is still demo-user-only), blank-id guard, `updateItemSchema.safeParse` as the validation source of truth (first Zod issue message returned), then the query call, returning a discriminated `{ success: true, data: ItemDetail } | { success: false, error }`. New `src/lib/validations/items.ts` — `updateItemSchema`: trimmed non-empty `title`; `description`/`language` trimmed with blank→null; `content` preserved verbatim (only ""→null) so code whitespace survives; `url` trimmed, blank→null, otherwise must pass `z.string().url()` (message "Enter a valid URL (including https://)"); `tags` array of trimmed non-empty strings, deduped via `Set`, `.default([])`. `src/lib/db/items.ts` gained `updateItem(id, data)`: demo-user `findFirst` ownership guard (→ `null` when not owned, which the action turns into "Item not found"), `prisma.item.update` replacing tags wholesale (`tags: { deleteMany: {}, create: [...connectOrCreate on name] }`), then returns a fresh `getItemDetail(id)` so the drawer refreshes with no second fetch. `ItemDrawer.tsx` was restructured: the detail-present branch is now `ItemDrawerContent`, keyed by item id upstream so it remounts (and resets) per item; it owns a `view`/`edit` toggle plus controlled field state seeded from `detail` on each entry into edit mode (so re-editing after a save uses fresh values). Edit mode replaces the action bar with Save/Cancel (Save disabled while `title.trim()` is empty), renders Title/Description/Tags always and Content (snippet·prompt·command·note) / Language (snippet·command) / URL (link) by type name, plus a read-only Collections + Created/Updated block; the old `ActionBar` is renamed `ViewActionBar` and its Edit button is now wired. On save success it calls `onSaved` (the provider's `setDetail` — new prop, Date fields survive the server-action round-trip so no revival needed), exits edit mode, toasts "Item updated", and `router.refresh()` so the card grid/rows re-render. Added shadcn `textarea` — same CLI bug as the alert-dialog in the Profile feature (`import { cn } from "cn"` + a stray `cn` dep); fixed the import to `@/lib/utils` and `npm uninstall cn`'d it, so no dependency change landed. New unit tests: `src/lib/validations/items.test.ts` (8, pure schema) and `src/actions/items.test.ts` (6, `vi.mock` on `@/auth` + `@/lib/db/items`). Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): snippet edit → toast, drawer returns to view with new title/description/tags (`react, hooks, storage, react` deduped to `react, storage, hooks`), `Updated` date bumped, card grid reordered with the new title and tag badges; re-entering edit re-seeds from the saved detail; whitespace-only title disables Save; Cancel discards; link item shows the URL field (not Content/Language) and an invalid URL keeps edit mode open with the "Enter a valid URL…" error toast; empty tags saves as `[]` (Tags section disappears); no console errors/warnings. One thing worth knowing: testing left the `useLocalStorage` seed item with its title/description/content/tags restored to seed values but its `updatedAt` now today (was Aug 21) — harmless ordering drift, no DB reset done. Build, lint, and all 42 tests passed.

- 2026-09-08: Completed Delete Item (`e91f926`, merged `d196a77`). The previously-inert Delete button in the item drawer's `ViewActionBar` now opens a shadcn `AlertDialog` ("Delete this item?" + the item title) before anything happens. `src/lib/db/items.ts` gained `deleteItem(id)`: demo-user `findFirst` ownership guard (→ `false` when not owned), then `prisma.item.delete` — `ItemTag` / `ItemCollection` join rows cascade via `onDelete: Cascade` in the schema, so no manual cleanup; returns `boolean`. `src/actions/items.ts` gained `deleteItem(itemId)` alongside `updateItem`, same shape: `auth()` signed-in gate ("You must be signed in to do that"), blank-id guard ("Invalid item"), try/catch ("Something went wrong…"), `false` from the query → "Item not found", new `DeleteItemResult = { success: true } | { success: false; error }`. No Zod — nothing but an id to check. Client wiring in `ItemDrawer.tsx`: the Delete `Button` becomes an `AlertDialogTrigger`; `AlertDialogAction` uses `preventDefault()` + manual `setConfirmOpen(false)` so the dialog stays open during the async call and on error (both buttons disable with a "Deleting…" label). On success: `onDeleted()` (a new prop threaded `ItemDrawer` → `ItemDrawerContent` → `ViewActionBar`, wired in `ItemDrawerProvider` to `setOpen(false)` + `setDetail(null)`), then `toast.success("Item deleted")` and `router.refresh()`. The `ViewActionBar` doc-comment was updated (Delete is now wired; only Favorite/Pin remain inert). New unit tests: `src/actions/items.test.ts` +5 `deleteItem` cases (unauth, blank id, success, not-found, DB throw), mirroring the `updateItem` block — 47 tests total. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`, `/items/snippets`): Delete opens the confirm dialog with the item title; Cancel closes only the dialog (drawer stays, nothing deleted); confirm → "Item deleted" toast, drawer closes, grid refreshes 4→3 cards; no console errors/warnings. Neon MCP confirmed demo items 18→17, the `Multi-stage Dockerfile` row gone, and 0 orphan `ItemTag` / `ItemCollection` rows (cascade worked). One thing worth knowing: verification permanently deleted the seeded `Multi-stage Dockerfile` snippet (was in the DevOps collection) — same verification-induced seed drift as the Edit Mode feature; `npm run db:seed` against a reset DB restores it. Minor accepted UX gap: pressing Escape while a delete is in flight closes the confirm dialog (Radix `AlertDialog` allows Escape even with Cancel disabled), but the request still resolves correctly. Build, lint, and all 47 tests passed.

- 2026-09-08: Completed Item Create (@context/features/item-create-spec.md) (`ab7ed02`, merged `06e0c50`). A shadcn `Dialog` opened by the top bar's "New Item" button is the item-creation flow — separate from the item drawer, which stays the detail/edit view. New `src/components/ui/dialog.tsx` was hand-authored to match `sheet.tsx`/`alert-dialog.tsx` (unified `radix-ui` import, `bg-popover` + `ring-foreground/10` Nova styling, `showCloseButton` prop) rather than run the shadcn CLI, which has repeatedly shipped a broken `import { cn } from "cn"` + stray `cn` dep in this repo. `src/lib/validations/items.ts` gained `CREATE_ITEM_TYPES` (`snippet`/`prompt`/`command`/`note`/`link` — `file`/`image` are Pro and need an upload flow) and `createItemSchema` = `updateItemSchema.extend({ type: z.enum(CREATE_ITEM_TYPES) })` plus a `.refine` enforcing "URL is required for links" (the `optionalUrl` transform already collapses blank→null, so the refine just checks `url !== null` when `type === "link"`). `src/lib/db/items.ts` gained `getCreatableItemTypes()` (the five types in fixed order, not user-scoped) and `createItem(data)`: resolves the type name to its system `ItemType` (→ null when missing, which the action reports as "Couldn't create item"), `prisma.item.create` with `contentType: "text"` (every creatable type is text-kind) and connect-or-create tags, then returns a fresh `getItemDetail(created.id)`. `src/actions/items.ts` gained `createItem(input)` alongside `updateItem`/`deleteItem`, same shape — `auth()` gate → `createItemSchema.safeParse` (first issue message) → query → `CreateItemResult = { success: true; data: ItemDetail } | { success: false; error }`. `src/components/items/CreateItemDialog.tsx` (client) is the modal: a row of type-toggle buttons (`ItemTypeIcon` + label, `aria-pressed`), then Title (required) / Description / Tags always, Content for snippet·prompt·command·note, Language for snippet·command, URL for link — mirroring the drawer's `CONTENT_TYPES`/`LANGUAGE_TYPES` split. Submit is disabled until `title` is non-blank (and `url` non-blank for links); on success it toasts "Item created", closes, resets, and `router.refresh()`. `TopBar.tsx` became an async server component that fetches the types via `getCreatableItemTypes()` and renders `<CreateItemDialog>` in place of the old display-only button (wrapped in `ml-auto`). New unit tests: `createItemSchema` (5, pure schema — valid payload, inherited title/tag rules, unknown/non-creatable type rejected, URL-required-for-links, link-with-URL) and `createItem` action (7, `vi.mock` on `@/auth` + `@/lib/db/items` with a new `createItemRecordMock`) — 59 tests total. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): New Item opens the dialog (Snippets preselected); switching to Links swaps Content/Language out for a required URL field; submit stays disabled with a title but no URL on links; created a link (`https://playwright.dev`, tags `testing, e2e, testing` deduped to `testing`/`e2e`) and a snippet (multi-line content + `typescript` language preserved) — each showed the "Item created" toast, closed the modal, bumped the Items stat, and appeared atop Recent; opening each in the drawer confirmed type, URL/content, language, and tags persisted. Both test items were deleted afterward via the drawer, so the dev database is back to its pre-test state (no seed drift this time). No console errors. Build, lint, and all 59 tests passed.

- 2026-09-08: Completed Code Editor (@context/features/code-editor-spec.md) (`18a45fc`, merged `5ca2ae5`). New `src/components/items/CodeEditor.tsx` (client) wraps Monaco via `@monaco-editor/react` (`dynamic(() => import(...), { ssr: false })`, per the library's documented Next App Router pattern) and replaces the plain `Textarea` (edit) and `<pre>` (view) for **`snippet` and `command`** items only — `prompt`, `note`, and everything else keep the `Textarea`/`<pre>`. The swap is gated by a new `CODE_TYPES = ["snippet", "command"]` constant inlined in both `ItemDrawer.tsx` and `CreateItemDialog.tsx` (same set as the existing `LANGUAGE_TYPES`, but kept as its own named constant for intent); `ItemDrawer`'s local `Field` helper had its `htmlFor` made optional to match `CreateItemDialog`'s, since the Monaco branch has no input id to associate a `<Label>` with. The component renders a bordered container with a macOS traffic-light header (three fixed-hex dots via Tailwind arbitrary-value classes, not inline styles) plus the raw language string and a Copy button (`navigator.clipboard` + sonner toast, mirroring `ViewActionBar`), then the editor. Height is fluid: an `onDidContentSizeChange` handler clamps `editor.getContentHeight()` to **96–400px** and drives the Monaco `height` prop; past 400 Monaco's own scrollbar takes over, themed through a `devstash-dark` theme defined in `beforeMount` (`base: "vs-dark"` + `scrollbarSlider.*` / `editor.background: #0d0d0d` overrides). `readOnly` (no `onChange`) switches between display and edit; a `toMonacoLanguage` alias map turns `bash`→`shell`, `ts`→`typescript` etc., with unknown ids falling back to `plaintext` (Monaco tolerates arbitrary ids anyway). No new server action / schema / query work — purely the content field's input control — so no new unit tests (components are out of the project's Vitest scope); the existing 59 pass unchanged. New deps: `@monaco-editor/react@4.7.0`, `monaco-editor@0.56.0` (the latter for types + peer; the Monaco runtime itself is fetched from a CDN by `@monaco-editor/react`'s default loader). Two things worth knowing: the CDN loader means the editor won't render offline / air-gapped — self-hosting is possible later but needs web-worker wiring under Turbopack, deferred; and `npm install` now reports 8 vulnerabilities (up from 6) in dev-only transitive deps — not acted on, consistent with the Vitest-setup decision. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): snippet drawer → read-only editor with dots + `typescript` label + Copy + syntax highlighting + 400px cap and scrollbar; snippet edit (content changed via the Monaco API, since Playwright's synthetic keystrokes don't reach Monaco's input area — a test-tool limitation, not an app bug) → Save persisted to the DB and the drawer returned to view mode; create dialog shows Monaco for Snippets/Commands and a plain `<textarea>` for Prompts; created a `bash` command (alias → `shell` highlighting), content + language persisted, then deleted it and restored the edited seed snippet via the UI. `Compound Component Pattern`'s `updatedAt` is now today (was Aug 21) — same accepted verification-induced seed drift as the Edit Mode / Delete features; `npm run db:seed` on a reset DB restores it. No console errors/warnings. Build, lint, and all 59 tests passed.

- 2026-09-08: Completed Markdown Editor (@context/features/markdown-editor-spec.md) (`a11f1f5`, merged `140991d`). New `src/components/items/MarkdownEditor.tsx` (client) — Write/Preview tabs — replaces the plain `Textarea` (edit) and `<pre>` (view) for **`note` and `prompt`** items; snippets/commands keep the Monaco `CodeEditor`, everything else keeps the `Textarea`/`<pre>`. Gated by a new `MARKDOWN_TYPES = ["note", "prompt"]` constant inlined in both `ItemDrawer.tsx` and `CreateItemDialog.tsx`, parallel to the Code Editor feature's `CODE_TYPES` — the content-field branch is now code → `CodeEditor`, markdown → `MarkdownEditor`, else `Textarea`, applied in all three surfaces (drawer edit, drawer view readonly, create dialog). Preview renders via `react-markdown` + `remark-gfm` (GFM tables, task lists, strikethrough, autolinks); edit mode shows both tabs defaulting to Write, readonly shows Preview only (no Write tab). Container `bg-[#1e1e1e]` / header `bg-[#2d2d2d]` with a Copy button mirroring `CodeEditor` (per the spec's stated hex values — note `CodeEditor` itself uses `#0d0d0d`, a minor inconsistency left as-specced). The Write control is a plain `<textarea>` (not shadcn `Textarea`) that auto-grows via a `scrollHeight` effect clamped at 400px; Preview is `max-h-[400px]` scroll with a "Nothing to preview." empty state. Rendered Markdown is styled by a hand-written `.markdown-preview` block appended to `globals.css` (unlayered plain CSS, so it beats Tailwind v4 Preflight's `@layer base` element resets without needing `@tailwindcss/typography`/`prose` — the spec explicitly wanted a custom class): distinct h1–h6 sizing/weight (rule under h1/h2), inline code (`oklch` white-alpha bg) + fenced `<pre>` (`#0d0d0d`), disc/decimal lists with markers + nested indent, GFM task-list checkboxes (`li:has(> input[type=checkbox])`), blockquote left border, blue links (`#60a5fa`) with hover underline, bordered tables with header bg, hr, responsive images. No new server action / schema / query work — purely the content field's input control — so no new unit tests (components are out of the project's Vitest scope); the existing 59 pass unchanged. New deps: `react-markdown@10.1.0`, `remark-gfm@4.0.1`; `npm install` still reports 8 dev-only transitive vulnerabilities (unchanged), not acted on. Verified via Playwright + a screenshot against the seeded dev database (signed in as `demo@devstash.io`): prompt drawer view → `MarkdownEditor` readonly (Preview label + Copy, no Write tab); edit mode → Write default with auto-grow textarea, Preview tab renders live; typed rich Markdown → confirmed `<h1>` 20.8px/600 with bottom border, ordered/unordered/nested lists, blockquote (2.4px left border + muted colour), inline + fenced `<pre><code>` (`#0d0d0d`), GFM `<table><thead>`, task-list checkboxes, blue links — CSSOM check confirmed all 31 `.markdown-preview` rules present and winning over Preflight; Save persisted to DB and the drawer returned to readonly view showing the new render; create dialog shows the Markdown editor (no Language field) for Notes/Prompts and Monaco + Language for Snippets/Commands. One thing worth knowing: an early "not rendering" report during review turned out to be a test prompt with no `#` heading syntax (correct flat rendering) plus a stale browser CSS cache — no code issue; confirmed working after a hard refresh with explicit heading syntax. Seed drift left by verification (not restored, per the user's call): `Refactoring Assistant`'s `updatedAt` is now today, and `Documentation Generator`'s content was replaced with `# Overview` / `# Section` test Markdown and its `updatedAt` bumped — `npm run db:seed` on a reset DB restores both. No console errors/warnings. Build, lint, and all 59 tests passed.

- 2026-09-08: Completed File & Image Upload with Cloudflare R2 (@context/features/file-image-spec.md) (`e60a9da`, merged `1e0ad9a`). The Pro-tier `file` / `image` item types are now creatable, with their body stored as an R2 object referenced by `Item.fileUrl` / `fileName` / `fileSize` + `contentType: "file"` (schema fields already existed, no migration). New `src/lib/r2.ts` wraps `@aws-sdk/client-s3` (new dep) pointed at `https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com` — a lazily-built singleton client plus `putR2Object` / `getR2Object` / `deleteR2Object`, `r2PublicUrl(key)` and `r2KeyFromUrl(url)` (recovers the key from a public URL, returns null when it isn't under `R2_PUBLIC_URL` so callers skip cleanup rather than delete the wrong object); `R2_BUCKET` is `""` when unconfigured and callers 503. New `src/lib/upload.ts` (+ 14 tests) is the shared, pure ruleset: `UPLOAD_CONSTRAINTS` (images 5 MB / png·jpg·jpeg·gif·webp·svg; files 10 MB / pdf·txt·md·json·yaml·yml·xml·csv·toml·ini), `validateUploadFile(kind, {name,size,type})` (extension gate first, then MIME — lenient on `""` / `application/octet-stream` since browsers omit types for md/toml/ini — then size), `buildObjectKey` (`items/<kind>/<uuid><ext>` via `crypto.randomUUID`), `formatFileSize`, `acceptAttr`. `POST /api/upload` (first non-auth `/api` route that writes): `auth()` → 503 if no bucket → `request.formData()` → `kind` + `File` checks → `validateUploadFile` (authoritative; client only mirrors it) → `PutObjectCommand` with the browser MIME → `{ success, data: { fileUrl, fileName, fileSize } }`. Upload-first flow (chosen over presigned-PUT or a combined create): the dialog uploads, then calls the existing `createItem` action with the returned metadata — so an object is orphaned in R2 if the dialog is cancelled after upload (accepted trade-off, noted). `GET /api/items/[id]/download` streams the object back via `object.Body.transformToWebStream()` with `Content-Type` from R2 and `Content-Disposition: attachment; filename="…"` (sanitised) — same-origin, so no R2 CORS config needed (a plain `<a href={R2_PUBLIC_URL}>` is CORS-blocked from the app origin, confirmed during testing). New `src/components/items/FileUpload.tsx` (client): drag-and-drop + click, uploads over `XMLHttpRequest` (not `fetch`) for real `upload.onprogress` → a `width: ${progress}%` bar, then an image `<img>` thumbnail or a file chip with name + `formatFileSize`, plus Remove; `onUploadingChange` lets the dialog disable submit mid-transfer; aborts the XHR on unmount / remove. `src/lib/validations/items.ts`: `FILE_ITEM_TYPES` (`file`,`image`) + `ALL_CREATE_ITEM_TYPES`; `createItemSchema` gains `fileUrl` / `fileName` / `fileSize` (nullish, blank→null) and a second `.refine` — file/image must carry all three ("Upload a file before saving"). `src/lib/db/items.ts`: `getCreatableItemTypes()` now returns all seven system types (was the five text types); `createItem` branches on `FILE_ITEM_TYPES` to write `contentType: "file"` + the file fields and null out content/url/language; `deleteItem` selects `contentType` + `fileUrl` and, after the row is deleted, best-effort `deleteR2Object(r2KeyFromUrl(fileUrl))` (failure logged, not fatal — the item is already gone). `CreateItemDialog.tsx`: file/image types render `<FileUpload>` in place of the content/language/url fields; a `handleTypeChange` clears any uploaded file on type switch; submit gated on `uploadedFile !== null && !uploading`. `ItemDrawer.tsx`: `ContentSection` delegates `contentType === "file"` to a new `FileSection` — inline `<img>` preview for `image` items, a filename + `formatFileSize` card, and a Download button (`<Button asChild><a href="/api/items/[id]/download" download>`); edit mode for file/image shows only Title/Description/Tags and leaves the file metadata untouched on save. `.env` / `.env.example`: `R2_BUCK_NAME` renamed to `R2_BUCKET_NAME` (the user had pre-added the R2 vars, uncommitted, with that typo + a truncated comment — both fixed). New tests: `src/lib/upload.test.ts` (14) and `src/lib/validations/items.test.ts` / `src/actions/items.test.ts` gained file-schema + file-create cases (the existing snippet-create assertions were updated for the three new always-present null fields) — 77 total, up from 59. `@aws-sdk/client-s3` added 25 packages; `npm install` still reports 8 dev-only transitive vulnerabilities (unchanged). Verified with Playwright against real Cloudflare R2 + the seeded dev database (signed in as `demo@devstash.io`): image upload → `POST /api/upload` 200, preview `<img>` loads from `pub-…r2.dev` (naturalWidth 1 for the 1×1 test PNG, so R2 public serving works), create → "Item created", drawer shows the preview + `pixel.png` / `69 B`; download proxy → 200 with `Content-Type: image/png` / `Content-Disposition: attachment; filename="pixel.png"` / 69 bytes, and `text/markdown` for a `.md` file item; client rejects a `.png` chosen in Files mode ("Files must be one of: …", no upload); edit-mode on a file item shows only title/description/tags and preserves the file through a save; delete → "Item deleted" **and** the R2 object returns 404 on its public URL (cleanup works), DB back to 17 items with zero orphan `ItemTag` / `ItemCollection` rows. Both test items were deleted afterward, so the dev database is back to its pre-test state (no seed drift). Build, lint, and all 77 tests passed.

- 2026-09-11: Completed Image Gallery View (@context/features/image-display-spec.md) (`3b8c0c9`, merged `7bf5c57`). New `src/components/items/ImageThumbnailCard.tsx` replaces `ItemCard` on `/items/images` only — the items grid in `src/app/items/[type]/page.tsx` now branches on `type.name === "image"` to render it instead, everything else keeps `ItemCard` unchanged. The card is a 16:9 (`aspect-video`, `overflow-hidden`) thumbnail using `object-cover`, with a `group`/`group-hover:scale-105` 300ms zoom (Tailwind v4 compiles this to the standalone CSS `scale` property, not `transform` — worth remembering when spot-checking hover effects via computed styles), an `ImageOff` fallback icon when `fileUrl` is null, and the same title/pin/favorite/date footer as `ItemCard`; same-origin `<img>` with the `@next/next/no-img-element` disable comment, matching `ItemDrawer.tsx`'s existing `FileSection` pattern. `ItemSummary` (`src/lib/db/items.ts`) gained a `fileUrl: string | null` field — the scalar was already being fetched by every `prisma.item.findMany`/`findFirst` call in that file (no `select` clause restricts it), just not mapped through `toItemSummary` before. No new server action / schema / query logic — purely a query field addition plus a display component — so no new unit tests; the existing 77 pass unchanged. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): `/items/images` (2 leftover test items from earlier sessions) rendered thumbnail cards with correct 16:9/object-cover computed styles; `/items/snippets` confirmed no regression to the regular `ItemCard` grid; hovering a thumbnail card confirmed `getComputedStyle(img).scale === "1.05"` under `@media (hover: hover)`. Build, lint, and all 77 tests passed.

- 2026-09-11: Completed File List View (@context/features/file-display-spec.md) (`d0ae3fb`, merged `8873d1b`). `/items/files` now renders a Drive/Dropbox-style single-column list instead of grid cards — a `type.name === "file"` branch in `src/app/items/[type]/page.tsx` (parallel to the Image Gallery View's `image` branch) wraps new `FileListRow`s in a `divide-y rounded-xl border bg-card` container; every other type keeps its grid. New `src/components/items/FileListRow.tsx` (client): extension-mapped file icon in a muted tile (`.pdf/.txt/.md` → FileText, `.json` → FileJson, `.xml/.yaml/.yml/.toml/.ini` → FileCode, `.csv` → FileSpreadsheet, fallback `File` — same extension set as `UPLOAD_CONSTRAINTS`), file name (falls back to title) with the usual pin/star, `formatFileSize`, upload date (`createdAt` via `formatItemDate`), and a ghost download button hitting the existing `GET /api/items/[id]/download` proxy with `stopPropagation` so it never opens the drawer. It's a client component using `useItemDrawer` directly rather than `ItemDrawerTrigger`, since an `<a>` can't legally nest inside the trigger's `<button>` — the row itself is a keyboard-accessible `role="button"` div (Enter/Space open the drawer). Below `sm` the size/date stack under the name; hover/focus-visible get the `accent/40` highlight. `ItemSummary` (`src/lib/db/items.ts`) gained `fileName`, `fileSize`, and `createdAt` (scalars already fetched by every query, just unmapped — same situation as the Image Gallery View's `fileUrl`), and `ItemDetail`'s now-duplicate declarations of those fields were removed since it inherits them; `getItemDetail`'s explicit re-assignments went too, covered by the `toItemSummary` spread. Two things worth knowing: the `react-hooks/static-components` lint rule rejects `const Icon = lookup(...)` during render even for a static map lookup (it can't see through the intermediate variable — plain `MAP[prop]` indexing like `ItemTypeIcon`'s passes), so the extension map stores pre-rendered JSX elements (`ReactNode`) instead of component references; and lucide's `FileJson` renders with class `lucide-file-braces` in the installed version — worth knowing when asserting icons via DOM class names. No new unit tests: the new logic is display-side (out of Vitest scope) and the db change is a pass-through field mapping. Verified via Playwright against the dev server (signed in as `demo@devstash.io`): row with correct icon/name/size/date, hover highlight (`oklab … / 0.4`), row click → drawer with full detail (confirming the `ItemDetail` refactor didn't break `GET /api/items/[id]`), download click → real file downloaded and drawer stayed closed, 400px viewport stacks the meta (`flex-direction: column`); created a `.json` file item to confirm distinct per-extension icons and the between-row divider, then deleted it via the drawer — DB back to its pre-test state, no console errors/warnings. Build, lint, and all 77 tests passed.

- 2026-09-11: Completed Quick Copy Icon on Item Cards (`bbfe061`, merged via `git merge --no-ff`). New `src/components/items/CopyItemButton.tsx` (client) — a ghost `icon-sm` Copy button that writes to `navigator.clipboard` with the same "Copied to clipboard" / "Couldn't copy" sonner toasts as the drawer's Copy action. `ItemCard.tsx` wraps the card in a `relative` container and renders the button absolutely positioned at the lower-right (`right-2.5 bottom-2.5`), as a **sibling** of `ItemDrawerTrigger` rather than a child — the trigger is a `<button>` and nesting another button would be invalid HTML, and the sibling overlay also means clicking Copy never opens the drawer with no `stopPropagation` needed. Copies `content ?? url` (text types' content, links' URL) and hides itself when both are null; the tags row gained `pr-8` so badges can't run under the icon. `ItemSummary` (`src/lib/db/items.ts`) gained `content` + `url` pass-through fields (scalars already fetched by every query — same precedent as `fileUrl` from the Image Gallery View), and `ItemDetail`'s duplicate `content`/`url` declarations plus `getItemDetail`'s explicit re-assignments were removed since they're inherited via the `toItemSummary` spread. Scope is `ItemCard` only — `ImageThumbnailCard` and `FileListRow` untouched (no text to copy; file rows already have a download action). No new unit tests: display-side logic (out of Vitest scope) plus a pass-through field mapping, same rationale as the Image Gallery / File List views. Verified via Playwright against the seeded dev database: snippet Copy → toast + clipboard holds the real code, drawer stays closed; link card copies `https://lucide.dev`; card body click still opens the drawer; button sits 10px in from the card's bottom-right; no console errors/warnings. Build, lint, and all 77 tests passed.

- 2026-09-11: Completed Fix Item Authorization & File URL Validation (`436af21`, merged via `--no-ff`) — the two actionable findings from that day's codebase-scanner audit. **Authorization (#1)**: any self-registered account could previously read, edit, delete, create, and download items belonging to the demo user — the item server actions (`src/actions/items.ts`) and `GET /api/items/[id]` / `GET /api/items/[id]/download` only checked that *someone* was signed in, then delegated to demo-user-scoped queries. `getItemDetail`, `createItem`, `updateItem`, and `deleteItem` in `src/lib/db/items.ts` now take an explicit owner `userId` as their first parameter (their internal `getDemoUserId()` calls removed); the actions and both routes pass `session.user.id`, and the routes' auth checks tightened from `session?.user` to `session?.user?.id`. The list/stat reads (`getPinnedItems`, `getRecentItems`, `getItemsByTypeSlug`, `getItemTypesWithCounts`, `getItemStats`) deliberately stay demo-scoped — that read gap remains the documented placeholder until the "wire dashboard to session user" feature. Consequence accepted: a non-demo account still sees demo items in lists but now gets "Item not found" opening/mutating them, and items it creates attach to *its own* user id. **File URL (#2)**: `createItem` accepted any non-blank `fileUrl` for `file`/`image` items (rendered directly in `<img>`), letting a caller bypass the upload flow and hotlink arbitrary external content; the action now rejects a `fileUrl` that `r2KeyFromUrl` can't resolve under the configured `R2_PUBLIC_URL` ("Invalid file URL"). The check lives in the action, not the Zod schema, to keep the schema free of env reads; it fails closed when R2 is unconfigured (correct — `/api/upload` 503s then, so no legitimate file items can exist). `updateItemSchema` has no file fields, so edit needed no equivalent. Tests: `src/actions/items.test.ts` asserts the session user id is threaded to all three record calls, mocks `@/lib/r2` at the module boundary, and gains hotlink-reject + R2-URL-accept cases — 78 total. Verified end-to-end via Playwright + Neon MCP against the dev database: as `demo@devstash.io` everything behaves as before (drawer, edit-save); as a freshly registered second account, the demo item API returns 404, the download route 404s, the drawer shows its error state with no data leaked, and a created item lands under the new account (confirmed by SQL). Cleanup afterward ran `npm run db:clean-users -- --yes`, which deleted all five non-demo users — including four pre-existing accounts (`troygurno@gmail.com`, `test10@gmail.com`, `turkeyman2@charter.net`, `tgurno2020.v@gmail.com`) beyond the test account, flagged to the user (restorable via Neon PITR if needed). Demo's `Compound Component Pattern` `updatedAt` bumped by the edit-save verification — usual accepted seed drift. Build, lint, and all 78 tests passed.

- 2026-09-11: Completed Audit Cleanup — Findings #3–#6 (`ba8db5a`, merged via `--no-ff`). Four maintainability/perf fixes from that day's codebase-scanner audit, deferring #7–#10 (component-size split, indicator-icon dedup, email FROM-address dedup, composite indexes) as lower priority. **#3 (type metadata duplication)**: `toLabel` and `SYSTEM_TYPE_ORDER`, previously copied byte-identical across `src/lib/db/collections.ts`, `src/lib/db/items.ts`, and `src/lib/db/profile.ts`, now live once in new `src/lib/item-types.ts`, imported by all three. **#4 (item-form UI duplication)**: the `CONTENT_TYPES`/`LANGUAGE_TYPES`/`CODE_TYPES`/`MARKDOWN_TYPES` type-group arrays and the `Field` label-wrapper component, both copied verbatim between `ItemDrawer.tsx` and `CreateItemDialog.tsx`, moved into `src/lib/item-types.ts` and new `src/components/items/ItemFormField.tsx` respectively; `CreateItemDialog`'s redundant local `FILE_TYPES` was also dropped in favor of the existing `FILE_ITEM_TYPES` export from `src/lib/validations/items.ts`. **#5 (collection query over-fetch)**: `COLLECTION_ITEMS_INCLUDE` in `collections.ts` switched from `include`-ing full `Item` rows (every scalar, including snippet/prompt bodies) to a `select` of just `itemType`, since `toCollectionSummary` only ever reads the type for its count/dominant-type computation — same query shape, far less data pulled on every dashboard/sidebar load. **#6 (unbounded queries)**: `getPinnedItems`, `getItemsByTypeSlug`'s items query, and `getFavoriteCollections` gained `take` limits (12, 100, 12 respectively, as named module constants) to match their already-capped siblings (`getRecentItems`, `getRecentCollections`, `getRecentNonFavoriteCollections`). Pure refactor — no behavior change intended anywhere; `toLabel` picked up a small unit test as a newly-standalone exported utility. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): dashboard type counts and Recent Collections render identically (5 collections, correct item counts/descriptions/dominant-type border colors), the New Item dialog and the drawer's edit mode both show the correct field set per type (Type/Title/Description/Content/Language/Tags for snippets), no console errors. Build, lint, and all 80 tests (78 + 2 new `toLabel` cases) passed.

- 2026-09-12: Completed Audit Cleanup — Findings #7–#10 + Component Size Review. Closed out the remaining findings from the codebase-scanner audit, deferred at the user's request when #3–#6 shipped. **General survey**: checked `CreateItemDialog.tsx` (280 lines) and `src/lib/db/items.ts` (405 lines) as the next-largest hand-written files — both already well-factored (one cohesive purpose, small internal functions), so left untouched; no other split candidates found. **#7 (ItemDrawer.tsx size)**: split the 649-line file (ten-plus components) into `ItemDrawerView.tsx` (`ItemDrawerBody`, `ContentSection`, `FileSection` — the view-mode display), `ItemDrawerEditForm.tsx` (`EditForm` + `ReadOnlyMeta` — the edit-mode fields and read-only meta block), `ItemDrawerActionBar.tsx` (`ViewActionBar` + its delete-confirm `AlertDialog`), and `ItemDetailSection.tsx` (the generic `Section` heading+content wrapper shared by the other two). `ItemDrawer.tsx` shrank to 272 lines, keeping its only externally-used export (`ItemDrawer`) plus the state-owning `ItemDrawerContent` and the loading components. Pure extraction, no behavior change. **#8 (pin/favorite icon duplication)**: new `src/components/items/ItemStatusIcons.tsx` exports `<ItemStatusIcons isPinned isFavorite />`, replacing the identical inline `Pin`/`Star` block previously copy-pasted in `ItemRow.tsx`, `ItemCard.tsx`, `ImageThumbnailCard.tsx`, and `FileListRow.tsx`. **#9 (email FROM_ADDRESS duplication)**: the hardcoded `"DevStash <onboarding@resend.dev>"` constant, identical in both `send-verification-email.ts` and `send-password-reset-email.ts`, now lives once in `src/lib/resend.ts` alongside the `resend` client export. **#10 (missing composite indexes)**: added `@@index([userId, isPinned])` and `@@index([userId, isFavorite])` to `Item` in `prisma/schema.prisma`, generated and applied via a real `prisma migrate dev` (never `db push`, per coding-standards.md) — migration `20260912092958_add_item_pinned_favorite_indexes`, confirmed against `.env`'s `DATABASE_URL` hostname (`ep-flat-queen-aycikso4`) matching the Neon `development` branch's connection string before running, `prisma migrate status` confirmed in sync afterward, and both indexes verified present via Neon MCP SQL (`production` was never touched). One incidental find during verification: Playwright's synthetic `.click()` intermittently failed to open the item drawer in this session (no console/page errors, DOM otherwise correct) — a raw `element.click()` via `page.evaluate` worked reliably every time, so verification used that instead; this reads as a test-tool/timing quirk, not an app regression, consistent with a similar Playwright click issue noted during the original Auth UI feature. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): drawer view mode (action bar, Monaco content, Copy), Edit → Cancel (fields correct, no mutation), Delete confirm dialog → Cancel (dialog closes, drawer stays open, item confirmed still in the database via Neon MCP), dashboard rows, `/items/files` and `/items/images` grids, and the New Item dialog all rendered correctly with no console errors. Build, lint, and all 80 tests passed.

- 2026-09-12: Completed Collection Create (`0000aab`, merged `--no-ff`). A "New Collection" button now sits next to "New Item" in `src/components/dashboard/TopBar.tsx`, opening `src/components/collections/CreateCollectionDialog.tsx` (client, shadcn `Dialog`) with Name (required) + Description (optional) fields. Unlike item mutations, client-side creation goes through a new `POST /api/collections` route (`src/app/api/collections/route.ts`) rather than a Server Action, per this feature's explicit spec — auth-gated (401 without `session.user.id`), validated against the new `createCollectionSchema` (`src/lib/validations/collections.ts`), calling a new `createCollection(userId, data)` in `src/lib/db/collections.ts`. On success: toast, dialog closes/resets, `router.refresh()` so the new collection shows up immediately. Broader than just the create path: `getRecentCollections`, `getFavoriteCollections`, `getRecentNonFavoriteCollections`, and `getCollectionStats` in `collections.ts` were migrated from the hardcoded demo user (`getDemoUserId()`) to an explicit `userId` parameter, mirroring the Sept 11 "Fix Item Authorization" change to `items.ts` — `Sidebar.tsx`, `StatsCards.tsx`, and `RecentCollections.tsx` now call `auth()` and pass `session.user.id` (falling back to empty/zeroed results when signed out, though in practice these routes are already proxy-gated). New unit tests: `src/lib/validations/collections.test.ts` (4, pure schema) — 84 total, up from 80. Deliberately out of scope: editing/deleting collections, adding items via this modal, and a `defaultTypeId` field on create. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): opened the modal, created "Playwright Test Collection" with a description, got the "Collection created" toast, saw it appear atop both the sidebar's and dashboard's Recent Collections and the Collections stat bump 6→7, no console errors — then deleted the test row via Neon MCP (explicit confirmation obtained first) so the dev database returned to its prior state. Build, lint, and all 84 tests passed.

- 2026-09-12: Completed Add Item to Collections (`3910653`, merged `--no-ff`). A "Collections" multi-select — toggle buttons in the same visual style as the existing Type selector — now appears on both the New Item dialog (`CreateItemDialog.tsx`) and the item drawer's edit form (`ItemDrawerEditForm.tsx`), letting an item be placed in zero, one, or many of the signed-in user's collections. New shared `src/components/items/CollectionMultiSelect.tsx` (client, no new deps) renders the picker off a plain `{id,name}[]` + selected-ids array; the drawer seeds/reseeds its selection from `detail.collections` on entering edit mode (mirroring the existing tags pattern), and the now-editable Collections moved out of `ReadOnlyMeta`'s view-only badge block into the main editable field list. `updateItemSchema` (inherited by `createItemSchema`) gained a deduped `collectionIds: string[]` field defaulting to `[]`; `src/lib/db/items.ts` gained a `resolveOwnedCollectionIds(userId, collectionIds)` helper — the security boundary, filtering the incoming ids down to ones `userId` actually owns before ever using them in a Prisma call, so a tampered id can never attach an item to someone else's collection — used by both `createItem` (connects on create) and `updateItem` (wholesale replace via `deleteMany` + recreate, same approach already used for tags). New `getCollectionsForSelect(userId)` in `src/lib/db/collections.ts` (+ exported `CollectionOption` type) feeds the picker's options; `TopBar.tsx`, `dashboard/page.tsx`, and `items/[type]/page.tsx` each now call `auth()` and fetch it themselves to pass down to `CreateItemDialog`/`ItemDrawerProvider` respectively, since `/items/[type]` doesn't share the dashboard's `TopBar`-bearing layout. Deliberately out of scope, per the spec: collection detail/listing pages (`/collections/[id]`, `/collections`). Updated `src/lib/validations/items.test.ts` and `src/actions/items.test.ts` for the new field — 85 total, up from 84. Verified via Playwright + Neon MCP against the seeded dev database (signed in as `demo@devstash.io`): created a snippet with two collections selected (DB confirmed both `ItemCollection` rows), opened it in the drawer to see both as read-only badges, re-entered edit mode to see both pre-selected as pressed toggle buttons, removed one and added a different one, saved, and confirmed via SQL the join rows updated to the new set with no stale rows; deleted the test item afterward (cascade confirmed, dev database back to its prior state). No console errors. Build, lint, and all 85 tests passed.

- 2026-09-12: Completed Collections Pages (`fe56f74`, merged `--no-ff`). Added `/collections` (all of the signed-in user's collections, reusing the existing `CollectionCard`) and `/collections/[id]` (the collection's items, reusing the existing `ItemCard`), both standalone pages following the `/profile`/`/items/[type]` pattern (no dashboard sidebar shell). `src/lib/db/collections.ts` gained `getAllCollections(userId)` and `getCollectionDetail(userId, id)` (both reusing `toCollectionSummary`/`COLLECTION_ITEMS_INCLUDE`), and `src/lib/db/items.ts` gained `getItemsByCollectionId(userId, collectionId)` — all three explicitly `userId`-scoped, not the demo-user hardcoding, per the pattern set by "Fix Item Authorization" and Collection Create. `/collections/[id]` 404s via `notFound()` when `getCollectionDetail` returns null (unknown or not-owned id), mirroring `/items/[type]`'s unknown-slug handling; the items query itself is a second, independent `userId` filter so it can never leak another user's items even if the ownership check were bypassed. The sidebar's "View all collections" link and `CollectionCard`'s own link already pointed at these routes from the Sept 6 Stats & Sidebar and Sept 7 Items List View features respectively — both were dead links until now, so no component changes were needed there. `src/proxy.ts`'s matcher gained `/collections/:path*`, alongside `/dashboard`, `/items`, and `/profile`. No new server-action/schema logic — purely new query functions mirroring existing patterns plus two display pages — so no new unit tests; the existing 85 pass unchanged. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): `/collections` listed all 6 collections with correct counts/dominant-type accents; `/collections/[id]` for "Terminal Commands" rendered its 4 mixed-type items as cards, and clicking one opened the full item drawer with correct detail; an unknown collection id correctly 404'd. Build, lint, and all 85 tests passed.

- 2026-09-12: Completed Collection Edit, Delete & Favorite (`f90b205`, merged `--no-ff`). `/collections/[id]` gained a `CollectionDetailActions` button row (Favorite/Edit/Delete) next to the title; `CollectionCard` (used on both `/collections` and the dashboard) gained a 3-dot `CollectionActionsMenu` dropdown with the same three actions. Favorite is display-only in both places — an inert icon/button reflecting `isFavorite`, no mutation wired, per the spec. Edit opens a fully-controlled `EditCollectionDialog` (name/description, re-seeded from the collection each time it opens via an "adjust state during render" check rather than a `useEffect`, which would have tripped `react-hooks/set-state-in-effect`) that `PATCH`s a new `/api/collections/[id]` route; Delete opens a controlled `DeleteCollectionDialog` (`AlertDialog`, explicit "items will not be deleted" copy) that `DELETE`s the same route, redirecting to `/collections` from the detail page or refreshing in place from a card. Both routes mirror Collection Create's existing route-based pattern (a route rather than a Server Action) and reuse a new `updateCollection`/`deleteCollection` pair in `src/lib/db/collections.ts` — delete only ever removes the `Collection` row itself; the schema's `onDelete: Cascade` on `ItemCollection` (not `Item`) already guarantees items survive and keep their other memberships, so no application code was needed to enforce that. `updateCollectionSchema` is a plain alias of `createCollectionSchema` (same name/description shape) rather than a duplicate. The one structural decision: `CollectionCard`'s dropdown trigger is an absolutely positioned sibling of the card's `Link`, not nested inside it — since the `Link` is non-positioned/in-flow, the positioned trigger paints above it and intercepts its own clicks with no `stopPropagation` needed, the same reasoning already used for `ItemCard`'s `CopyItemButton`. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): created a throwaway collection, edited its name/description from both the card dropdown and the detail page (fields correctly re-seeded, toast, live update), confirmed the card's dropdown never triggers navigation while the rest of the card still does; added an item to that collection plus "Design Resources", deleted the collection (confirmation dialog showed the expected copy), and confirmed the item still existed — now only in Design Resources — both on that collection's page and on `/items/snippets`; cleaned up the test item and collection afterward, dev database back to its prior state (22 items, 6 collections). No console errors. Build, lint, and all 85 tests passed.

- 2026-09-12: Completed Global Search / Command Palette (@context/features/global-search-spec.md) (`cfad9d7`, merged `--no-ff`). A Cmd/Ctrl+K command palette now fuzzy-searches a signed-in user's items and collections from the dashboard shell. New `src/components/ui/command.tsx` hand-authors shadcn's `cmdk`-based `Command` primitives (the CLI has repeatedly shipped a broken `cn` import in this repo, per several prior features) rather than running the generator; `cmdk` is a new dependency. `src/components/search/SearchProvider.tsx` owns the palette's open state and a global `keydown` listener for the shortcut, exposing `useSearchPalette()` so any descendant can open it; `CommandPalette.tsx` renders the grouped Items/Collections results (cmdk's own default fuzzy matching and keyboard nav — no custom filtering code needed) and handles selection: an item calls `openItem(id)` on the existing item-drawer context, a collection calls `router.push`. `SearchTrigger.tsx` replaces the TopBar's previously display-only search `Input` with a button styled the same way that opens the palette on click. The key structural change: `ItemDrawerProvider`, previously instantiated only inside `dashboard/page.tsx` (wrapping just `PinnedItems`/`RecentItems`), is now hoisted up into `dashboard/layout.tsx` wrapping the whole shell (`Sidebar` + `TopBar` + `children`), so a palette-selected item can open the drawer regardless of what's rendered on the page — `dashboard/page.tsx` simplified accordingly with no behavior change for its existing consumers. Two new lightweight, purpose-built queries feed the palette rather than reusing the heavier existing list functions: `getSearchableItems(userId)` (`src/lib/db/items.ts`, id/title/type/a 140-char preview from description-or-content, capped at 500) and `getSearchableCollections(userId)` (`src/lib/db/collections.ts`, id/name/itemCount via a `_count` select, skipping `COLLECTION_ITEMS_INCLUDE`'s per-item type join since the palette doesn't need it) — both fetched once by `dashboard/layout.tsx` per page load, not per keystroke. Deliberately scoped to the dashboard shell: `/items/[type]`, `/collections`, `/collections/[id]`, and `/profile` have never had the sidebar/TopBar chrome (a prior, unrelated decision), so the palette and its shortcut aren't available there. No new unit tests — display-layer component plus `src/lib/db/*.ts` query functions, which this project has never unit-tested (only Prisma-free pure logic gets extracted and tested); the existing 85 pass unchanged. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): Ctrl+K and the TopBar trigger both open the palette; typing "docker" correctly fuzzy-matched a command item and hid the empty Collections group; selecting an item opened it in the real item drawer with full detail; selecting "DevOps" navigated to `/collections/[id]`; `/items/snippets` (outside the dashboard shell) unaffected. No console errors. Build, lint, and all 85 tests passed.

- 2026-09-12: Completed Pagination (@context/features/pagination-spec.md) (`2c5de3a`, merged `--no-ff`). `/items/[type]`, `/collections/[id]`, and `/collections` are now paginated instead of relying on flat safety-cap `take` limits (100 items, 100 collections). New `src/lib/pagination.ts` centralizes `ITEMS_PER_PAGE`/`COLLECTIONS_PER_PAGE` (21) and the dashboard's `DASHBOARD_COLLECTIONS_LIMIT`/`DASHBOARD_RECENT_ITEMS_LIMIT` (6/10 — previously local constants duplicated in `RecentCollections.tsx`/`RecentItems.tsx`), plus pure `parsePage`/`getTotalPages`/`getSkip` helpers (8 unit tests). New `src/components/shared/PaginationControls.tsx` renders numbered page links with an ellipsis-collapsed window around the current page, plus prev/next `Link`s that render as disabled (greyed out) `Button`s at the ends. `getItemsByTypeSlug`, `getItemsByCollectionId` (`src/lib/db/items.ts`), and `getAllCollections` (`src/lib/db/collections.ts`) all gained a `page` parameter and switched from a flat `take` to `skip`/`take`, so each page request only fetches that page's rows; `getItemsByTypeSlug`/`getAllCollections` also run a parallel `count` query for the total, while `/collections/[id]` reuses `getCollectionDetail`'s already-computed `itemCount` instead of a second count query. All three pages read `?page=` via `parsePage` and `notFound()` on an out-of-range page, mirroring `/items/[type]`'s existing unknown-slug 404. The one scope decision: the spec's bullet list only named `/items/[type]` and `/collections/[id]`, but it also defined `COLLECTIONS_PER_PAGE` with no page to apply it to — `/collections` is the collections listing that constant implies, and the spec's overview explicitly says "items **and collections** listings", so it got paginated too (confirmed with the user before implementing). Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): temporarily set both page-size constants to 2 to exercise real multi-page behavior against the small seed set — `/items/snippets` (4 items → 2 pages, correct items per page, prev/next disabled at each end, `?page=5` → 404), `/collections` (6 collections → 3 correctly-numbered pages), `/collections/[id]` (4-item collection → 2 pages) — then reverted to the spec's real value of 21, confirming every real listing (all under 21 items/collections) renders with no pagination nav at all. No console errors. Build, lint, and all 93 tests (85 + 8 new) passed.

- 2026-09-12: Completed Settings Page (`7a596fd`, merged `88b4c4e`). New protected `/settings` page (`src/proxy.ts` matcher gained `/settings/:path*`) now holds the Change Password and Delete Account cards, moved off `/profile` — `/profile` keeps only the Account info card and `ProfileStats`. `ChangePasswordForm.tsx`/`DeleteAccountDialog.tsx` were relocated from `src/components/profile/` to a new `src/components/settings/` to match the feature-based file-organization convention; no logic changes to either. `SidebarUser.tsx`'s user dropdown gained "Profile" and "Settings" links (order: Profile, Settings, Sign out — Profile was added in a follow-up since it had previously only been reachable via the gear icon), and the gear icon (`SidebarMenuAction`) now points at `/settings` instead of `/profile`, confirmed with the user. One resolved ambiguity: the request said "forgot password" as one of the actions to move, but `/profile` never had forgot-password UI — only "Change password" (`ChangePasswordForm`) — so that's what moved; the standalone signed-out `/forgot-password` flow is untouched. No server actions or utilities changed, so no new unit tests; the existing 93 pass unchanged. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): dropdown shows Profile/Settings/Sign out in order, Settings link and gear both navigate to `/settings` with Change Password + Danger Zone rendered, `/profile` confirmed to no longer show either card, and an unauthenticated request to `/settings` correctly 307-redirected to `/sign-in?callbackUrl=...`. Build, lint, and all 93 tests passed.

- 2026-09-12: Completed Editor Preferences Settings (@context/features/editor-settings-spec.md) (`110d3e6`, merged `745fa39`). Added a font size / tab size / word wrap / minimap / theme (`vs-dark`/`monokai`/`github-dark`) section to `/settings`, auto-saved on every change with a success/error toast — no save button. `User` gained an `editorPreferences Json?` column (migration `20260912155357_add_user_editor_preferences`, applied via a real `prisma migrate dev` against the `development` branch, confirmed by hostname first); `src/lib/validations/editor-preferences.ts` is the Zod schema + `DEFAULT_EDITOR_PREFERENCES` + a fail-open `parseEditorPreferences` (malformed/null stored JSON silently falls back to defaults rather than throwing) — 8 unit tests. `src/lib/db/editor-preferences.ts` / `src/actions/editor-preferences.ts` follow the existing query-function + auth-gated-server-action pattern. The one structural decision: `EditorPreferencesProvider` is mounted once in the **root layout** (`src/app/layout.tsx`, now async and calling `auth()`), not the dashboard layout — `CodeEditor` is also reachable from `/items/[type]` and `/collections/[id]`, neither of which shares the dashboard's shell, so a single dashboard-scoped provider would have missed them. `CodeEditor.tsx` was changed to read `useEditorPreferences()` directly instead of taking props, so the item drawer's view mode, its edit mode, and the New Item dialog all picked up the wiring with zero call-site changes; its previous single hardcoded `devstash-dark` Monaco theme became three named themes (`devstash-vs-dark`/`devstash-monokai`/`devstash-github-dark`), each `defineTheme`'d with hand-picked token-rule colors matching the real Monokai/GitHub Dark palettes. New `select.tsx` and `switch.tsx` UI primitives were hand-authored against the unified `radix-ui` package rather than run through the shadcn CLI, matching the precedent set by `dialog.tsx`/`command.tsx` in prior features (the CLI has repeatedly shipped a broken `cn` import in this repo). One incidental fix: a stale dev server process from an earlier session had an in-memory Prisma client from before the migration and returned a 500 on every page (`Unknown field editorPreferences`) — resolved by killing it and starting fresh after `prisma generate`, unrelated to the app code itself. Verified via Playwright against the seeded dev database (signed in as `demo@devstash.io`): toggling each control auto-saved with a toast and survived a full page reload; switching to Monokai + enabling minimap changed the *view*, *edit*, and *create-dialog* Monaco instances' background color and minimap presence identically (confirmed via computed styles, `rgb(39, 40, 34)` = `#272822`); font size 18 measured as `18px` on a rendered `.view-line` glyph. Reset the demo account back to defaults afterward (13px/2-space/`vs-dark`/word-wrap-on/minimap-off) — no lasting state change. Build, lint, and all 101 tests (93 + 8 new) passed.
