/**
 * Phase 1 placeholder. Item-type shortcuts, collections and the user footer
 * arrive in a later phase.
 */
export function Sidebar() {
  return (
    <aside className="hidden w-64 shrink-0 border-r bg-sidebar text-sidebar-foreground md:block">
      <div className="flex h-16 items-center px-6">
        <h2 className="text-sm font-medium">Sidebar</h2>
      </div>
    </aside>
  );
}
